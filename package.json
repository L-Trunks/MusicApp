{
  "name": "musicapp",
  "private": true,
  "description": "MusicApp 根目录脚本：本地/生产构建",
  "scripts": {
    "build": "docker compose build",
    "build:production": "docker compose --env-file .env.production build"
  }
}
